/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.pinimg.com', 'upload.wikimedia.org','image/webp', 'image/avif', 'image/png', 'image/jpeg'], // Add any other domains you need
    },
    
  };
export default nextConfig;
